/*
💥 Generally "app.js" use to be the entry point in node application.
*/

const math = require("./math"); // ◽ require() is an function used to import one module into another

console.log(math.add(20, 20));
